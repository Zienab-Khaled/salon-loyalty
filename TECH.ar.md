# الخطة التقنية — رفع مجاني على Vercel

## لماذا Vercel؟
- خطة مجانية كافية لصالون واحد في البداية.
- رفع Next.js سهل (ربط GitHub أو أمر `vercel`).
- ما نحتاج استضافة أو VPS.

## الستاك المقترح (مرحلة أولى)

| الطبقة | الاختيار | ملاحظة |
|--------|----------|--------|
| الواجهة + API | **Next.js** (App Router) | مناسب لـ Vercel |
| قاعدة بيانات | **Vercel Postgres** أو **Supabase** (مجاني) | جداول عملاء + زيارات |
| ملفات/صور | مجلد `public/` | الشعار وصورة الثيم |
| QR | `qrcode.react` أو مشابه | توليد من رقم العضوية |
| إشعارات | **Resend** (إيميل مجاني محدود) | ثم واتساب لاحقاً |
| خط عربي | Cairo / Tajawal (Google Fonts) | RTL |

بديل أخف لو حابين بدون قاعدة بيانات معقدة في البداية:
- **Firebase** (Auth خفيف + Firestore) — برضو يشتغل مع موقع على Vercel.

## صفحات الموقع

```
/                  → صفحة تعريفية قصيرة + دخول العميل
/card/[code]       → بطاقة الولاء للعميل (الثيم)
/staff             → لوحة الموظف (محمية)
/staff/scan        → مسح / إدخال رقم + إضافة تحليقة
```

## تدفق إضافة تحليقة

```
الموظف يمسح QR
    → يفتح /staff مع معرف العميل
    → POST /api/stamps (محمي)
    → يزيد stamps في قاعدة البيانات
    → يرسل إشعار للعميل
    → البطاقة تظهر التقدم الجديد
```

## خطوات الرفع على Vercel (بعد ما يتبني الكود)

1. ارفع المشروع على GitHub (مستودع خاص أو عام).
2. ادخل [vercel.com](https://vercel.com) وسجّل بحساب GitHub.
3. Import Project → اختر المستودع.
4. أضف متغيرات البيئة (رابط قاعدة البيانات، كلمة مرور الموظف، مفتاح الإيميل…).
5. Deploy — يعطيك رابط جاهز مثل `gentlemen-loyalty.vercel.app`.
6. (اختياري) اربط دومين خاص لاحقاً.

أمر بديل من الجهاز:

```bash
npx vercel
```

## حدود الخطة المجانية (مهم تعرفها)
- تكفي تجربة وتشغيل صالون صغير/متوسط في البداية.
- لو زاد الضغط أو الإيميلات، نرقّي الخدمة المعنية فقط (DB أو Resend) بدون تغيير فكرة الموقع.

## ترتيب البناء المقترح
1. بطاقة ثابتة بنفس الثيم (UI فقط).
2. توليد QR + رقم عضوية.
3. تسجيل عميل بسيط + حفظ التقدم.
4. لوحة موظف + إضافة ختم.
5. إشعار عند الإضافة.
6. ربط Vercel + تجربة حقيقية في الصالون.
